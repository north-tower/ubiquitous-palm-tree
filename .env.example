# Base URL of the Nest API (server-side proxy in app/backend/[...path]/route.ts).
# Local dev: backend on port 3000 or docker compose API on host port 5600.
API_URL=http://localhost:5600

# Host port when using docker compose (container listens on 3001).
UI_PORT=3001
